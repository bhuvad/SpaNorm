% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpuFunctions.R
\name{toGPUVector}
\alias{toGPUVector}
\title{Convert a vector to a torch tensor on the active backend}
\usage{
toGPUVector(vec, n = NULL, backend = c("auto", "cpu", "gpu"))
}
\arguments{
\item{vec}{a numeric vector (or an existing torch tensor, returned as-is).}

\item{n}{if supplied, the target length; a length-1 \code{vec} is
broadcast to length \code{n}.}

\item{backend}{one of \code{"auto"} (default), \code{"cpu"} or \code{"gpu"}.}
}
\value{
a torch tensor, or \code{vec} unchanged.
}
\description{
As \code{\link{toGPUMatrix}}, for a numeric vector; optionally broadcasts a
length-1 \code{vec} to length \code{n}.
}
\examples{
toGPUVector(1:4, backend = "cpu")

}
