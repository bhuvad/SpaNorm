% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quasilik.R
\name{nbUnitDeviance}
\alias{nbUnitDeviance}
\title{NB unit deviance}
\usage{
nbUnitDeviance(y, mu, phi)
}
\arguments{
\item{y}{counts, genes x cells (matrix or torch tensor).}

\item{mu}{fitted means, same shape (matrix or torch tensor).}

\item{phi}{NB dispersion: a scalar, or one value per gene (row).}
}
\value{
a genes x cells matrix, or a torch tensor when the input was one.
}
\description{
The negative binomial unit deviance
\eqn{2\{y \log(y/\mu) - (y + 1/\phi) \log((y + 1/\phi)/(\mu + 1/\phi))\}},
elementwise over a genes x cells matrix, on the CPU or -- when \code{y}
or \code{mu} is a torch tensor -- on that tensor's device.
}
\examples{
y <- matrix(rpois(20, 3), 4, 5)
nbUnitDeviance(y, mu = y + 0.5, phi = 0.1)
}
