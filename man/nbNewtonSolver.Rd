% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nbSolver.R
\name{nbNewtonSolver}
\alias{nbNewtonSolver}
\alias{nbNewtonSolverBatch}
\alias{nbGramBatch}
\alias{nbAbsorbGramBatch}
\title{Penalised NB Newton solvers and batched grams (for package developers)}
\usage{
nbNewtonSolver(W, pen, absorb = NULL)

nbNewtonSolverBatch(W, pen, absorb = NULL)

nbGramBatch(
  W,
  wt_block,
  penalty_diag = NULL,
  backend = "cpu",
  cell.tile = NULL
)

nbAbsorbGramBatch(W, pen, absorb, wt_block, cell.tile = NULL, parts = FALSE)
}
\arguments{
\item{W}{a cells x p design (base matrix, or torch tensor for the batch forms).}

\item{pen}{a length-p ridge penalty.}

\item{absorb}{see Description.}

\item{wt_block}{a genes x cells weight matrix (one row per gene).}

\item{penalty_diag, backend, cell.tile, parts}{see \code{polishNB()}.}
}
\value{
\code{nbNewtonSolver()}: a list of closures \code{factor(w)},
\code{solve(state, s)} (the Newton step, \code{NULL} if singular) and
\code{xcov(state)} (the dense-block covariance). The batch forms return
the batched grams or Schur complements.
}
\description{
Low-level building blocks of \code{\link{polishNB}}, exported so that a
downstream package (e.g. spiDE) can form the penalised covariance of a
polished fit from the SAME factorisation the polish used. The information is
\code{W' diag(w) W + diag(pen)}. \code{absorb} marks an indicator block whose
part of the information is block-diagonal and is eliminated by a Schur
complement: \code{NULL} (dense), a logical over \code{W}'s columns (each
marked column its own 1x1 block), or an integer block id per column
(\code{NA} = dense).
}
\examples{
W <- cbind(1, seq(-1, 1, length.out = 20))
s <- nbNewtonSolver(W, pen = c(0, 1))
st <- s$factor(rep(1, 20))
s$solve(st, c(1, 0))
}
\keyword{internal}
