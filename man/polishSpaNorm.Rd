% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polishSpaNorm.R
\name{polishSpaNorm}
\alias{polishSpaNorm}
\alias{polishSpaNorm,SpatialExperiment-method}
\alias{polishSpaNorm,Seurat-method}
\title{Converge each gene of a SpaNorm fit to its own optimum}
\usage{
polishSpaNorm(
  spe,
  adj.method = c("auto", "logpac", "pearson", "medbio", "meanbio"),
  scale.factor = 1,
  psi.method = c("fixed", "profile"),
  ls = c("fixed", "joint"),
  cells = c("all", "fit"),
  null = TRUE,
  maxit = 50L,
  tol = 1e-08,
  engine = c("batch", "gene"),
  batch.size = NULL,
  backend = c("cpu", "auto", "gpu"),
  BPPARAM = BiocParallel::SerialParam(),
  overwrite = FALSE,
  verbose = TRUE,
  assay = NULL,
  ...
)

\S4method{polishSpaNorm}{SpatialExperiment}(
  spe,
  adj.method = c("auto", "logpac", "pearson", "medbio", "meanbio"),
  scale.factor = 1,
  psi.method = c("fixed", "profile"),
  ls = c("fixed", "joint"),
  cells = c("all", "fit"),
  null = TRUE,
  maxit = 50L,
  tol = 1e-08,
  engine = c("batch", "gene"),
  batch.size = NULL,
  backend = c("cpu", "auto", "gpu"),
  BPPARAM = BiocParallel::SerialParam(),
  overwrite = FALSE,
  verbose = TRUE,
  assay = NULL,
  ...
)

\S4method{polishSpaNorm}{Seurat}(
  spe,
  adj.method = c("auto", "logpac", "pearson", "medbio", "meanbio"),
  scale.factor = 1,
  psi.method = c("fixed", "profile"),
  ls = c("fixed", "joint"),
  cells = c("all", "fit"),
  null = TRUE,
  maxit = 50L,
  tol = 1e-08,
  engine = c("batch", "gene"),
  batch.size = NULL,
  backend = c("cpu", "auto", "gpu"),
  BPPARAM = BiocParallel::SerialParam(),
  overwrite = FALSE,
  verbose = TRUE,
  assay = NULL,
  ...
)
}
\arguments{
\item{spe}{a SpatialExperiment or Seurat object that \code{\link[=SpaNorm]{SpaNorm()}} has been
run on, with the raw counts in its 'counts' assay (layer, for Seurat).}

\item{adj.method}{a character, specifying the method used to rewrite the
normalised data from the polished fit (default 'auto'), as in
\code{\link[=SpaNorm]{SpaNorm()}}.}

\item{scale.factor}{a numeric, specifying the scaling factor for the
adjusted counts, as in \code{\link[=SpaNorm]{SpaNorm()}}.}

\item{psi.method}{how each gene's dispersion is set: \code{"fixed"} (the
default) keeps the fit's dispersion and converges the mean under it;
\code{"profile"} re-estimates it by profile maximum likelihood at the
converged mean and re-polishes (see \code{\link[=polishNB]{polishNB()}}).}

\item{ls}{the library-size coefficient shared by every gene: \code{"fixed"} (the
default) holds it at the fit's value. \code{"joint"}, which re-estimates it
jointly with the per-gene coefficients, is not implemented yet.}

\item{cells}{the cells/spots each gene is converged over: \code{"all"} (the
default), or \code{"fit"} for the ones \code{\link[=SpaNorm]{SpaNorm()}} sampled to fit the model.}

\item{null}{a logical, specifying whether to also polish the null model
stored by \code{\link[=SpaNormSVG]{SpaNormSVG()}} ('SpaNormNull'), when there is one (default
TRUE). The SVG test compares the two fits, so they must be estimated the
same way.}

\item{maxit, tol}{the Newton iteration cap and the relative log-likelihood
tolerance, per gene.}

\item{engine, batch.size, backend}{passed to \code{\link[=polishNB]{polishNB()}}: the batched or
per-gene Newton engine, genes per batch, and the compute backend.}

\item{BPPARAM}{a BiocParallelParam object over gene blocks, for the polish
(see \code{\link[=polishNB]{polishNB()}}) and the normalisation (default
\code{BiocParallel::SerialParam()}).}

\item{overwrite}{a logical. A fit that is already polished is refused
unless this is TRUE (default FALSE); with TRUE it is polished again,
starting from the unpolished fit kept as 'SpaNormUnpolished' (and
'SpaNormNullUnpolished'), never from the polished one.}

\item{verbose}{a logical, specifying whether to show update messages
(default TRUE).}

\item{assay}{a character, specifying the assay holding the raw counts for
Seurat objects (default NULL uses the object's default assay). Ignored
for SpatialExperiment objects.}

\item{...}{further arguments passed to \code{\link[=polishNB]{polishNB()}}, such as \code{block.size}
or \code{psi.range}.}
}
\value{
a SpatialExperiment or Seurat object holding the polished fit(s),
with the normalised data rewritten in 'logcounts' or 'data',
respectively.
}
\description{
\code{\link[=SpaNorm]{SpaNorm()}} fits every gene in one shared IRLS loop, with a gene-averaged
cell weight vector and an aggregate convergence criterion, so a gene can be
left short of its own optimum (bright, spatially restricted genes most of
all). \code{polishSpaNorm()} takes the stored fit and converges each gene
separately under SpaNorm's own model, stores the polished fit, and rewrites
the normalised assay from it.
}
\details{
SpaNorm's mean for gene \eqn{g} in cell/spot \eqn{i} is
\deqn{\log \mu_{gi} = \bar{\mu}_g + a_1 w_{i1} + \sum_{j \ge 2} W_{ij}
  \alpha_{gj},}{log mu_gi = gmean_g + a1 * W_i1 + sum_{j >= 2} W_ij
  alpha_gj,} where \eqn{w_{i1}} is the log library size and its
coefficient \eqn{a_1} is shared by every gene. The polish maximises each
gene's penalised negative binomial log-likelihood by damped Newton (see
\code{\link[=polishNB]{polishNB()}}), with \eqn{a_1} held at the fit's value (\code{ls = "fixed"}),
\eqn{\bar{\mu}_g}{gmean_g} a per-gene unpenalised intercept, and the
other coefficients ridge-penalised as \code{\link[=SpaNorm]{SpaNorm()}} penalises them: by
\code{lambda.a} for the biology and library-size splines, not at all for
batch, times the number of cells/spots.

The polished coefficients are the optimum of the unwinsorised penalised
likelihood. The normalisation (and \code{\link[=SpaNormSVG]{SpaNormSVG()}}) still applies
SpaNorm's usual winsorisation, exactly as for an unpolished fit: it forms
the mean through \code{\link[=calculateMu]{calculateMu()}}, which by default winsorises the upper
tail of each gene's log mean, and caps the dispersions at
\code{exp(median(log psi) + 4 MAD(log psi))}.

A gene with no counts in the polished cells/spots has no finite optimum
(its intercept runs to minus infinity), so it is not polished: it keeps
its input coefficients and dispersion, and its diagnostics row reads
\code{polished = FALSE} with \code{iterations = 0}. A gene the Newton engine
cannot converge from either start also keeps its input fit (see
\code{\link[=polishNB]{polishNB()}}).

The polished fit replaces 'SpaNorm' in the object's metadata (\verb{@misc}
for Seurat) and the input fit is kept as 'SpaNormUnpolished'. With
\code{null = TRUE} a stored 'SpaNormNull' is polished with the same settings
and the input kept as 'SpaNormNullUnpolished'. SVG results in \code{rowData}
were computed from the unpolished fit, so they are removed with a
warning; rerun \code{\link[=SpaNormSVG]{SpaNormSVG()}}. The normalised assay ('logcounts', or the
'data' layer for Seurat) is rewritten from the polished fit.

The polished fit's \code{polish} slot (see \code{\link[=isPolished]{isPolished()}}) holds \code{settings}
(\code{psi.method}, \code{ls}, \code{cells}, \code{maxit}, \code{tol}, the penalty vector \code{pen},
the library-size coefficient before (\code{a1.input}) and after (\code{a1}), and
the SpaNorm version) and \code{genes}, one row per gene: \code{\link[=polishNB]{polishNB()}}'s
diagnostics plus \code{loglik}, the penalised log-likelihood over the polished
cells at the returned fit (\code{NA} for a gene that was not polished). The
fit's \code{loglik} slot is left as the shared fit's iteration trace.

The negative binomial likelihood is defined on counts only, so a
non-integer counts assay (for example a back-transform such as
\code{2^logcounts - 1}) is refused.
}
\examples{
data(HumanDLPFC)
\donttest{
top <- order(-Matrix::rowSums(SummarizedExperiment::assay(HumanDLPFC, "counts")))[1:50]
spe <- SpaNorm(HumanDLPFC[top, ], sample.p = 0.05, df.tps = 2, tol = 1e-2)
spe <- polishSpaNorm(spe)
isPolished(S4Vectors::metadata(spe)$SpaNorm)
}
}
\seealso{
\code{\link[=SpaNorm]{SpaNorm()}}, \code{\link[=polishNB]{polishNB()}}, \code{\link[=isPolished]{isPolished()}}.
}
